
<link href="~/Content/landing/comingSoon.css" rel="stylesheet" />

@if (Request.IsAuthenticated)
{
    <section class="questionContent">
        <div class="row">
            <div class="col-md-12 col-sm-12">
                <div class="row text-center">
                    <h4>Добро пожаловать!</h4>
                </div>
            </div>
        </div>
    </section>
}
else
{
    <section class="questionContent">
        <div class="row">
            <div class="col-md-9 col-sm-9">
                Чтоб подать заявку  зарегестрируйтесь.
            </div>
            <div class="col-md-9 col-sm-9">
                <button id="myBtn">Условия соглашения</button>
                <div id="myModal" class="modal">
                    <div class="modal-content">
                        <span class="close">&times;</span>
                        <p>Условия соглашения..</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

}

    <h2 style="text-align:center">Галлерея</h2>

    <div class="container">
        <div class="mySlides">
            <div class="numbertext">1 / 3</div>
            <img src="../../../../Content/img/img_nature_wide.jpg" style="width:100%">
        </div>

        <div class="mySlides">
            <div class="numbertext">2 / 3</div>
            <img src="../../../../Content/img/img_snow_wide.jpg" style="width:100%">
        </div>

        <div class="mySlides">
            <div class="numbertext">3 / 3</div>
            <img src="../../../../Content/img/img_mountains_wide.jpg" style="width:100%">
        </div>

        <a class="prev" onclick="plusSlides(-1)">❮</a>
        <a class="next" onclick="plusSlides(1)">❯</a>

        <div class="caption-container">
            <p id="caption"></p>
        </div>
    </div>

    @section scripts{
        <script type="text/javascript">
        var modal = document.getElementById("myModal");
        var btn = document.getElementById("myBtn");
        var span = document.getElementsByClassName("close")[0];
        btn.onclick = function () {
            modal.style.display = "block";
        }
        span.onclick = function () {
            modal.style.display = "none";
        }
        window.onclick = function (event) {
            if (event.target == modal) {
                modal.style.display = "none";
            }
        }



        var slideIndex = 1;
        showSlides(slideIndex);

        function plusSlides(n) {
            showSlides(slideIndex += n);
        }

        function currentSlide(n) {
            showSlides(slideIndex = n);
        }

        function showSlides(n) {
            var i;
            var slides = document.getElementsByClassName("mySlides");
            var dots = document.getElementsByClassName("demo");
            var captionText = document.getElementById("caption");
            if (n > slides.length) { slideIndex = 1 }
            if (n < 1) { slideIndex = slides.length }
            for (i = 0; i < slides.length; i++) {
                slides[i].style.display = "none";
            }
            for (i = 0; i < dots.length; i++) {
                dots[i].className = dots[i].className.replace(" active", "");
            }
            slides[slideIndex - 1].style.display = "block";
            dots[slideIndex - 1].className += " active";
            captionText.innerHTML = dots[slideIndex - 1].alt;
        }

        </script>
    }
